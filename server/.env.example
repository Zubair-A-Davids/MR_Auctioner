# Copy to .env and fill these in
PORT=8787
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mr_auctioner
JWT_SECRET=change_me_to_a_long_random_secret
# Comma-separated list of allowed origins (e.g., your GitHub Pages custom domain)
CORS_ORIGINS=https://mr-auctioner.co.za,https://www.mr-auctioner.co.za
